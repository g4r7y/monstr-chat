{
  "name": "monstr-chat",
  "version": "1.0.0",
  "scripts": {
    "test": "npx tsc --noemit && node --experimental-test-module-mocks --import tsx --test **/*.test.ts",
    "install": "npm install --prefix terminal",
    "build:terminal": "cd terminal && npm run build",
    "start:terminal": "cd terminal && npm run start",
    "go": "npm run install && npm run build:terminal && npm run start:terminal"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@nostr/tools": "npm:@jsr/nostr__tools@2.17.1",
    "@scure/bip39": "1.2.1"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "tsx": "4.20.3",
    "typescript": "^5.8.3"
  }
}
