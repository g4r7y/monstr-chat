{
  "name": "monstr-chat",
  "version": "1.0.0",
  "scripts": {
    "test": "npx tsc --noemit && node --experimental-test-module-mocks --import tsx --test **/*.test.ts",
    "install": "npm install --prefix terminal",
    "build:terminal": "cd terminal && npx tsc",
    "build": "npm run build:terminal",
    "start:terminal": "npm run build:terminal && cd terminal && node dist/terminal/src/main",
    "start": "npm run start:terminal"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@nostr/tools": "npm:@jsr/nostr__tools@2.17.1",
    "@scure/bip39": "1.2.1"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "tsx": "4.20.3",
    "typescript": "^5.8.3"
  }
}
