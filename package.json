{
  "name": "monstr-chat",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run test --workspace=core && npm run test --workspace=terminal",
    "build": "npm run build --workspace=terminal && npm run build --workspace=web-app"
  },
  "workspaces": [
    "core",
    "web-app",
    "terminal"
  ],
  "license": "Apache-2.0"
}
